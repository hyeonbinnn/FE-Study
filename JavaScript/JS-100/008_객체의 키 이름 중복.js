var d = {
  height: 180,
  weight: 78,
  weight: 84,
  temperature: 36,
  eyesight: 1,
};

console.log(d['weight']);
// 출력: 84

// JavaScript 객체 내에서 동일한 속성 이름을 가진 프로퍼티를 정의할 때, 나중에 정의된 프로퍼티가 이전에 정의된 프로퍼티를 덮어쓰게 됨
