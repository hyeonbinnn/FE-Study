// 깨어진 돔의 조각

// * documentFragment
// 오직 메모리상에서만 존재하는 경량화된 DOM을 만들 수 있음

// 1. DocumentFragment를 DOM 노드에 추가한다고 해도 DocumentFragment 노드는 등록되지 않고 그 자식 노드들만 추가됨
// 2. DocumentFragment를 DOM에 추가하면 DocumentFragment 노드의 자식 요소들은 더 이상 메모리상에 존재하지 않음
